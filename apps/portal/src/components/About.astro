---
const skillGroups = [
  { title: "前端", items: ["TypeScript", "React", "Astro", "Tailwind CSS"] },
  { title: "后端", items: ["Node.js", "Cloudflare Workers", "D1", "PostgreSQL"] },
  { title: "工具", items: ["Git", "Docker", "Figma"] },
];
---

<section id="about" class="px-5 py-20 sm:px-6 sm:py-28">
  <div class="mx-auto max-w-5xl">
    <p class="font-mono text-[13px] text-accent">01 — 关于</p>
    <h2 class="mt-4 text-3xl font-semibold tracking-tight text-ink sm:text-4xl">关于我</h2>

    <div class="mt-12 grid grid-cols-1 gap-12 lg:grid-cols-12 lg:gap-10">
      <!-- 简介 -->
      <div class="lg:col-span-6">
        <p class="max-w-prose text-lg leading-relaxed text-ink-muted">
          我是一名全栈开发者，主要用 TypeScript 和 Node.js 做产品。比起堆功能，我更在意一个界面是否顺手、一段代码是否清晰。
        </p>
        <p class="mt-5 max-w-prose text-lg leading-relaxed text-ink-muted">
          这个站点跑在 Cloudflare 上，是我做实验和记录的地方。除了这个主页，我还维护着一个匿名聊天室——没有别的原因，就是想有个地方，让路过的人能说上几句话。
        </p>
      </div>

      <!-- 技能分组 -->
      <div class="lg:col-span-5 lg:col-start-8">
        <div class="flex flex-col divide-y divide-line">
          {skillGroups.map((g) => (
            <div class="flex flex-col gap-2.5 py-5 first:pt-0 last:pb-0 sm:flex-row sm:items-baseline sm:gap-6">
              <span class="w-14 flex-shrink-0 font-mono text-xs uppercase tracking-widest text-ink-faint">
                {g.title}
              </span>
              <span class="text-[15px] text-ink-muted">{g.items.join(" · ")}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
